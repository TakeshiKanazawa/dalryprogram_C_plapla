  // // y - r が0 より小さい、または、y + r がH より大きいならばNoと判定。
  // if (y - r < 0 || y + r > H) {
  //   cout << "No\n";
  //   return 0;
  // }
  // // y -r が0 以上、かつ、y + r がH 以下ならばYesと判定。
  // if (y - r >= 0 || y + r <= H) {
  //   cout << "Yes\n";
  //   return 0;
  // }